# Public origin of this deployment. Drives the page metadata, the /api/v1
# index and every generated code snippet.
#
# NEXT_PUBLIC_ means Next inlines it at build time, so changing it needs a
# rebuild rather than just a restart — which is what a Coolify redeploy does.
NEXT_PUBLIC_SITE_URL=https://tools.truvibe.dev

# Dev only. Extra hostnames the dev server is reached through, comma separated.
#
# `pnpm dev` binds 0.0.0.0, and Next blocks cross-origin requests to dev-only
# assets from any origin not listed here — so if you reach the dev server as
# anything other than localhost, put that hostname in. Wildcards are allowed.
#
# It lives here rather than in next.config.ts so the repo does not publish
# anyone's dev host. Unset in production, where Next ignores the option anyway.
# ALLOWED_DEV_ORIGINS=dev.example.com,*.example.com

# Redis backing the public API rate limiter.
# If unset, the app falls back to an in-process limiter — fine for local dev,
# wrong for production (resets on redeploy, and is per-replica).
#
# Locally, point it at any Redis you already have; keys are namespaced
# trutools:rl: so sharing one across projects is fine. Deployed it is
# redis://redis:6379 too, but as service-name DNS inside the compose stack,
# set by docker-compose.yml rather than here.
REDIS_URL=redis://localhost:6379

# Set this when a CDN in front is the authority on the caller's address.
#
# Needed whenever TLS terminates at the origin rather than at the proxy that
# saw the caller: an L4 passthrough or tunnel cannot add X-Forwarded-For, and
# Traefik rewrites the incoming one from the tunnel peer, so the chain only
# holds private hops. Cloudflare's own header rides through untouched.
#
#   Cloudflare -> passthrough/tunnel -> Traefik   CLIENT_IP_HEADER=cf-connecting-ip
#
# Leave unset without a CDN in front: nothing would stop a caller sending the
# header themselves. With one, also restrict the origin to the CDN's ranges,
# or the same bypass exists for anyone who reaches it directly.
# CLIENT_IP_HEADER=cf-connecting-ip

# Set to 1 to expose /ip?debug in production. It lists every forwarding header
# that arrived and marks each chain entry public or private, which is how you
# find out why a reported address is wrong. On automatically outside production.
# IP_DEBUG=1

# Public API rate limit, per IP. Defaults to 60 requests per 60 seconds.
RATE_LIMIT_MAX=60
RATE_LIMIT_WINDOW_SEC=60
